# python program to find length of last word in a string

def length_of_last_word(s):
    words = s.split()                    # Split the string into word
    last_word = words[-1]                # extract the last word 
    last_word_length = len(last_word)    # Extract the length of the last word
    print(last_word_length)              # print length of last word


""" 
call the function to any string 
example :
s1 = "Hello World"
print(length_of_last_word(s1))  # Output: 5

s2 = "   fly me   to   the moon  "
print(length_of_last_word(s2))  # Output: 4

s3 = "luffy is still joyboy"
print(length_of_last_word(s3))  # Output: 6

"""
